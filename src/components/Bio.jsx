import { bio } from "../Data";

const Bio = () => {
  return (
    <section className="text-base text-gray-900 max-w-[380px] text-center md:text-lg">
      {bio}
    </section>
  );
};
export default Bio;
